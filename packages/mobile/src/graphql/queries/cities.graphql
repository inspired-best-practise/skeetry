query Cities(
  $continent: String,
  $isCapital: Boolean,
  $after: String,
  $before: String,
  $first: Int!,
  $input: CitiesInput,
  $last: Int,
  $orderBy: CityOrder,
  $query: String,
  $skip: Int
) {
  cities(
    continent: $continent,
    isCapital: $isCapital,
    after: $after,
    before: $before,
    first: $first,
    input: $input,
    last: $last,
    orderBy: $orderBy,
    query: $query,
    skip: $skip
  ) {
    edges {
      cursor
      node {
        ...RegularCity
      }
    }
    pageInfo {
      ...RegularPageInfo
    }
    totalCount
  }
}