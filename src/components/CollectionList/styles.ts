import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  collectionListTitle: {
    paddingHorizontal: 18,
    paddingVertical: 18,
    fontWeight: '600',
    fontSize: 16,
  },
});
