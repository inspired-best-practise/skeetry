import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  filterItem: {
    backgroundColor: '#eee',
    borderRadius: 6,
    paddingHorizontal: 20,
    paddingVertical: 12,
    marginHorizontal: 6,
  },
  filterItemTitle: {
    fontSize: 14,
    fontWeight: '600',
  },
});
